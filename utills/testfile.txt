#include <iostream>
#include <unistd.h>
#include "master.hpp"
#include <fstream>
#include <functional>

int main(int, char**)
{
    int size = 15;
    // Create loading bar without error reporting
    utills::loading bar1(size);
    sleep(2);
    for (int i = 0; i < size; ++i)
    {
        bar1.update();
       // sleep(1);
    }
size = 10;

    std::cout << "\nSecond loading bar with error reporting:\n";
    utills::errorReport er;
    er.setErrorFile("../errorFile.txt", true);
    er.setError("test error");
    utills::loading bar2(size, std::ref(er)); // Pass a reference wrapped in std::ref
    sleep(1);
    for (int i = 0; i < size; ++i)
    {
        bar2.update();
        //sleep(1);
    }
    utills::fileManager testFile("abc123.txt", er);
ofstream o;
o.open("abc123.txt");
o << "hello, world";
    er.printErrors();
   for (int i = 0;i < 100;i++) {er.setError(";)");er.writeErrors();}
    er.printErrors();
    
    return 0;
}